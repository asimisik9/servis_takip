[pytest]
testpaths = tests
asyncio_mode = auto
addopts = -ra
markers =
    unit: fast unit tests that mock external dependencies
    e2e: end-to-end backend API smoke tests against a running stack
    slow: slower tests that require external services or startup time
